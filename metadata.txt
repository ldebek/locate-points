# This file contains metadata for your plugin. Since 
# version 2.0 of QGIS this is the proper way to supply 
# information about a plugin. The old method of 
# embedding metadata in __init__.py will 
# is no longer supported since version 2.0.

# This file should be included when you package your plugin.# Mandatory items:

[general]
name=Locate points along lines
qgisMinimumVersion=2.0
description=Creating points along lines with given offset and interval
version=0.2
author=Łukasz Dębek
email=damnback333@gmail.com

# End of mandatory metadata

# Recommended items:

# Uncomment the following line and add your changelog:
changelog=0.2
 - New "Add vertices" option;
 - Setting interval to 0 will cause adding vertices only (startpoints, endpoints or all vertices - depends on the options checked);
 - Other minor code changes.
 <p>0.1.2
 - Fixed distance values for endpoints when offset is greater than line length and 'Add endpoints' flag is checked.
 <p>0.1.1
 - Fixed distance calculations for endpoints.
# Tags are comma separated with spaces allowed
tags=distance, geometry, feature, layers, point, polyline, vector

homepage=https://github.com/DamnBack/locate-points
tracker=https://github.com/DamnBack/locate-points/issues
repository=https://github.com/DamnBack/locate-points
category=Plugins
icon=icon.png
# experimental flag
experimental=False

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False
